# FirstLight_CR 联机自动化项目全景梳理

更新时间：2026-09-10 晚
撰写背景：用户要求全面梳理项目内容、联机模式已完成的工作、当前问题与后续计划。

---

## 一、项目是什么

FirstLight_CR 是一个 Clash Royale（皇室战争）AI 项目，基于 Null's Royale 私服。
完整目标：**AI 模型在联机模式下与服务器匹配的真实对手自动化对战。**

项目由两部分构成：

### 1. 作者已开源的部分（文档齐全，已全部验证可用）

| 组件 | 位置 | 作用 | 验证状态 |
|---|---|---|---|
| 原生探针 probe | `native_runner/probe/cr_replay_probe.cpp`（约 9000 行 C++） | 注入游戏引擎 libg.so，挂钩读取对局状态、执行动作 | ✅ |
| 构建部署 | `offline_build.py`、`offline_install.py`、`build_offline_engine.ps1` | 反编译 APK→注入 probe→签名→安装 | ✅ |
| 引擎通信 | `cr_native_env.py` | TCP 26789 控制协议（session-v1） | ✅ |
| 对局环境 | `battle_env.py`、`contracts.py` | 把引擎状态转成 FAIR ObservationV1 观测、ActionV1 动作 | ✅ |
| 策略模型 | `training/v4/`（V4 checkpoint） | 读观测→每 5 tick 决策→输出动作 | ✅（模型已有：General/IL） |
| 策略服务 | `training/v4/serve_policy.py`（文档 `policy_service.md`） | 本地 JSON 行协议推理服务 | ✅ |
| 交互界面 | `user_interface.py`、`launch_interface.cmd` | 四页面控制台：模型对战/手动对战/回放 | ✅ |
| 离线评测 | `training/v4/evaluate.py` | headless 跑完整对局 | ✅ 已跑通（winner:a, tick 5375） |

### 2. 作者未开源、需自行补齐的部分

**联机模式的对局状态定位**。离线模式下 probe 自己创建对局并通过
`loadReplay` 挂载世界（world），一切结构可控；联机模式下对局由服务器驱动，
world 的存放位置与离线不同，作者没有公开这部分实现。

---

## 二、系统架构（离线模式，已验证）

```
MuMu 模拟器 (Android 12, ARM64 转译)
  └─ Null's Royale 私服 APK + 注入 libcrprobe.so
       └─ libg.so (游戏引擎)
            ├─ probe 钩子: 读对局状态 → 观测 JSON
            │      ↓ TCP 26789 (adb forward)
            └─ 接收 ActionV1 → 引擎内执行出牌
                    ↑
PC (Windows)
  └─ battle_env → tensorizer → V4 模型 → ActionV1
```

离线链路一次通过：本地引擎执行对局，probe 读状态，模型决策，
动作回注。**这条链路的所有环节今天都是完好的。**

---

## 三、联机模式：今天完成的工作

### 阶段 0：稳定性修复（全部完成，多局验证零崩溃）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 私服 APK 缺 Google Play 评分组件，对局开始/结束转场必崩 | libg 引用 `com.google.android.play.core.review.*`，APK 未打包 | 加 5 个空壳 smali 类（`offline_build.py` 的 `add_play_review_stubs`） |
| 2 | live-observe 解除时未关战斗遥测 epoch | 悬挂指针指向已释放管理器 | 解绑时同步 `end_combat_event_epoch` |
| 3 | 出牌时 consume-card 钩子读非法 world（联机管理器 +0xa8 存的是阶段标志 0x1，非指针） | `read_player_state` 无输入校验 | 函数边界防御：拒绝不可读/非指针输入 |
| 4 | 战场实体采集读已释放对象 | 对象从模拟移除但向量陈旧条目仍在，内存已被复用（浮点垃圾） | `capture_combat_entity` 可读性门控 |

### 阶段 1：联机数据源打通（核心已验证）

| 数据 | 状态 | 获取方式 |
|---|---|---|
| 联机对局管理器捕获 | ✅ | 替换式 live attach：armed 期间新对局管理器首次 step 即绑定 |
| 对局 tick | ✅ | `g_game_state_tick(manager)`，实测正常跳 |
| 双方玩家归属与在场单位数 | ✅ | 遍历 objectManager 对象数组（+0x08/+0x10/+0x14，离线验证的布局），按 owner（+0x78）分组 |
| 己方圣水 | ✅ 完整曲线 | player 对象+0x2f8（1.33→4.78→骤降→恢复，与实际出牌吻合） |
| 对手圣水 | ✅ 完整曲线（候选择优+锁定后） | 同上，8.48→9.72→骤降至 1.32 的完整轨迹已捕获 |
| 玩家对象定位 | ✅ | 从战场对象字段反查：候选需通过"player+0x10==objectManager && +0x220 手牌向量非空 && 圣水 ∈ [-1,100000]"三重校验，择优锁定 |
| 战斗遥测钩子触发 | ✅ 确认 | damage/spawn/consume-card 等钩子在联机对局中触发 |

### 关键技术发现（联机与离线的差异）

| 项 | 离线模式 | 联机模式 |
|---|---|---|
| world 指针 | manager+0xa8 | **不在 manager+0xa8**（装载期为 0x0，模拟启动后出现 0x1 等非指针值） |
| loadReplay | 触发（probe 自己喂对局） | 不触发（服务器驱动） |
| 对象+0x100 | 实体引用 | 卡牌 UI 实体（三重复结构，内含竞技场格坐标浮点），不是玩家 |
| 对局生命周期 | probe 控制 | 服务器控制，对象随时生成/销毁 |

---

## 四、当前遇到的问题（1 个，但它是结构性的）

### 问题：钩子读取"对象内部引用字段"时的生命周期竞争

**现象**：多局验证后仍偶发开局闪退（今天累计 7 次，已修复 5 种触发点）。

**根因**（今天最后一次崩溃精确定位）：战斗遥测钩子会顺着战场对象内部的
**引用字段**继续读其他对象（如"该单位的目标"、"伤害来源"）。当游戏释放
某个对象后，那块堆内存被复用——原指针字段变成浮点垃圾（崩溃点读到的
`0x3f400014` 就是浮点 0.75+0x14）。钩子把垃圾当指针解引用 → SIGSEGV。

**为什么"打地鼠"修不完**：这类引用字段解引用散布在 4 个遥测模块
（combat/remaining_runtime/character_state/phase_runtime）共 **160+ 处读取点**。
每次只修崩溃的那一条，下一局就在另一条路径崩。

**为什么可读性检查也挡不住**：内存页释放后通常仍处于映射状态（检查通过），
但内容已被覆写为垃圾。`process_range_is_readable` 的 250ms 地图缓存进一步
扩大了这个窗口。

**结论**：逐点修补不可行，需要结构性方案（见第六节选项）。

---

## 五、数据源现状清单（模型所需）

| 数据 | 联机现状 | 备注 |
|---|---|---|
| tick / 对局时间 | ✅ 已验证 | |
| 双方圣水 | ✅ 已验证（完整曲线） | player+0x2f8，需候选锁定防污染 |
| 在场单位（位置/血量/归属/卡牌） | 🟡 objectManager 数组可遍历（owner+0x78、坐标+0x7c/0x80、卡牌+0xac 已知），血量偏移离线有（+0x14 链），**联机下未逐项验证** | |
| 己方手牌/卡组 | 🟡 player 对象已定位，偏移已知（+0x220/+0x88），联机下未逐项验证 | |
| 对手手牌 | 按 FAIR 规则不可直接读（与真实游戏一致），通过对手出牌事件推断 | |
| 对手出牌事件流 | 🟠 combat 钩子触发已确认，但采集入库依赖上述引用字段读取（当前崩溃源），受结构性问题阻塞 | |
| 对局结束/胜负 | 🔴 未解决（依赖 world，未定位；可用"管理器停止步进"替代信号） | |


---

## 七、可回退性说明

- probe 是单个 .so 文件，任何版本回退/前滚都是一次 dd 部署（分钟级）；
- 网络隔离（离线/联机切换）是纯 iptables 规则操作，可无限次来回切换；
- APK 层面的评分组件修复是必要的（与联机崩溃无关），无需回退；
- 游戏账号数据在私服服务器上，本地操作不影响。

## 八、当前部署状态

- 设备 probe：`88a4933b`（含全部 4 项稳定性修复 + 候选择优锁定）
- 已安装 APK：`a1dbf029`（评分组件修复版）
- 已知问题：联机对局中，战斗遥测钩子仍可能在开局阶段触发偶发闪退
  （结构性问题，见第四节）
- 稳定版本参考：评分修复后、live 观测加入前的版本全程零崩溃
